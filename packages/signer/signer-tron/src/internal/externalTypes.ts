export const externalTypes = {
  Dmk: Symbol.for("Dmk"),
  SessionId: Symbol.for("SessionId"),
  // Phase 3: add ContextModule for clear-signing
  // ContextModule: Symbol.for("ContextModule"),
} as const;
